{
    "bootstrap": "../../vendor/autoload.php",
    "path": "./",
    "reports": {
        "bench": {
            "extends": "aggregate",
            "break": ["params"],
            "cols": [ "benchmark", "mean", "diff", "worst", "best", "mem_real" ],
            "sort": {"mean": "asc"}
        }
    }
}
